@model SSVH_Consultation_Poratl.Models.Stationary

@{
    ViewData["Title"] = "Delete Uniform";
}

<div class="row">
    <div class="col-12">
        <div class="custom-table">
            <div class="row">
                <div class="col-12">
                    <h1 class="text-center mb-3">Delete Uniform</h1>
                </div>
            </div>
            <div>
                <dl class="row">
                    <div class="col-3"></div>
                    <div class="col-6">
                        <h4 class="text-center mb-2">Are you sure you want to delete this Uniform?</h4>
                        <hr />
                        <div class="row">
                            <table class="table table-striped table-responsive table-hover">
                                <tbody>
                                    <tr>
                                        <th scope="row" class="w-50">Class Name</th>
                                        <td>@Model.ClassName</td>
                                    </tr>
                                    <tr>
                                        <th scope="row" class="w-50">Amount</th>
                                        <td>@Model.Amount</td>
                                    </tr>
                                    <tr>
                                        <th scope="row" class="w-50">Gender</th>
                                        <td>@Model.Gender</td>
                                    </tr>
                                </tbody>
                            </table>
                            <div class="col-12">
                                <div class="form-group" style="display:flex; float:right">
                                    <a class="btn btn-outline-primary me-2" asp-action="Index">Back to List</a>
                                    <form asp-action="Delete" method="post">
                                        <input type="hidden" asp-for="Id" />
                                        <div class="form-group">
                                            <input type="submit" value="Delete" class="btn btn-outline-danger" />
                                        </div>
                                    </form>
                                </div>
                            </div>
                        </div>
                    </div>
                    <div class="col-3"></div>
                </dl>
            </div>
        </div>
    </div>
</div>
